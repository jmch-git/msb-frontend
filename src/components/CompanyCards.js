function CompanyCards(props) {
    return (
        <div>
            <h2>Company Cards</h2>
{/* //**********REVIEW and UNCOMMENT after Backend Deployment************           
            <img src={props.image} alt={props.name} />
            <a href={props.website} target="_blank">{props.name}</a>
            <p>{props.industry}</p>
            <textarea name="companyDesc" cols="30" rows="10">{props.description}</textarea> */}
        </div>
    );
}

export default CompanyCards;